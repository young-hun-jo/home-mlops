pipeline {
    agent any
    stages {
        stage('Check pwd') {
            steps {
                sh 'pwd && ls -l'
            }
        }
        stage('Move dir') {
            steps {
                sh 'cd serving && pwd && ls -l'
            }
        }
    }
}
