pipeline {
    agent any
    
    environment {
        APP_NAME = 'tabular-diabetes-regressor'
    }

    stages {
        stage('Moving directory') {
            steps {
                sh 'cd ${APP_NAME} && pwd && ls -l'
            }
        }
    }
}
