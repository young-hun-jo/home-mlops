service: "main:svc"
labels:
    onwer: zedd-ai
    stage: local
include:
- "*.py"
python:
    packages:
    - scikit-learn
    - numpy
    - mlflow  # BentoML Serve에서 mlflow trained model 사용하려면 반드시 mlflow 설치해야 함
models:
- zedd-iris-clf-in-mlflow:2gsbniavxw47hbxq  # 로컬이 미리 bentoml Model Store에 등록해놓은 mlflow 모델 및 tag를 명시